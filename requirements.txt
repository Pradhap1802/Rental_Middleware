fastapi==0.141.1
uvicorn==0.52.1
requests==2.34.2
pydantic==2.13.4
cryptography==50.0.0
python-multipart==0.0.9
APScheduler==3.11.3

# Required to run service.py as a Windows Service from source (not needed when
# running the compiled exe, which bundles it via PyInstaller's hidden imports).
pywin32==312; sys_platform == "win32"

# Test dependencies
pytest==9.1.1
